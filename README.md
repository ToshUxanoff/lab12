Программа для создания асинхронного запроса по заданному URL к HTTPS серверу.

Сборка:
```ShellSession
$ cmake -H. -B_build -DCMAKE_INSTALL_PREFIX=_install
$ cmake --build _build --target install
```
